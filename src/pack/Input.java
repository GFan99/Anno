package pack;

/**
 * Dies wird die Klasse Input.
 * Input dient dazu die bereitgestellte XML-Datei zu lesen und daraus die zu bearbeitenden Texte
 * mit den gewünschten Labeln abzuleiten.
 * Sie hat Zugriff auf die Klasse Klassifikator, damit sie dort in das label[] - Array die Namen
 * der Label und in das text[] - Array die zu bearbeitenden Texte eintragen kann.
 * Inout ist die erste Klasse, die von Steuerung angesprochen wird, da sie für das Laden der Da-
 * ten zuständig ist, mit denen das Programm dann arbeiten soll.
 * Zusätzlich kann sie die Dateien lesen, die erstellt werden, falls das Programm vor abarbeiten
 * aller Texte beendet wird. Dies dient dazu, dass der Nutzer mit dem Klassifizieren/Labeln dort
 * weitermachen kann, wo er zuvor aufgehört hat, ohne einen Text doppelt zu bearbeiten.
 * @author susannabeck
 * @version 0.1
 */
public class Input {

}
