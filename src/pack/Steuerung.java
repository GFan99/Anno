package pack;

public class Steuerung {
	
	public static String nutzerID;

	public static void main(String[] args) {
		/**
		 *  ToDo: 
		 *   - frage Nutzer nach seiner ID (hinweis geben, wie zu verfahren ist bei keiner id (leer 
		 *     lassen ?) oder extra button?
		 *   - prüfe, ob Speicherdatei mit nutzerid bereits vorhanden --> wenn ja laden
		 *   - lade sonst xml-eingabe und generiere neue nutzerid --> wie? mit hashfkt um langen Zahlen- 
		 *     und Buchstaben enthaltenden Code zu enthalten? zufallszahl + (statt *) nr des nutzers zum
		 *     hashen nehmen? oder komplett zufällig bilden? hashfkt direkt auf nr des nutzers anwenden?
		 *   - variablen zuordnen
		 *   - fenster starten (statisch: nutzerid-anzeige, schließ-knopf (?), textfeld (mit scrolleiste!?),
		 *     ok-button, fortschrittsbalken bzw prozentangabe; dynamisch: auswahl"knöpfe" und labelname-
		 *     feld) --> textgröße manuell einstellbar???
		 *   - timer starten (eine Stunde) --> auch im fenster anzeigen
		 *   - text auswählen und angaben in feld eintragen
		 *   - wdh bis texte alle oder fenster geschlossen
		 *   - texte alle --> anzeige, dass fertig; dann ausgabe-xml schreiben (mit nutzerid als titel?)
		 *   - nutzer schließt fenster --> kleines fenster mit fortschritt; dann nutzerid.datei speichern
		 *   - programm beenden
		 */

	}

}
